// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}

model User {
  id            String         @id @default(cuid())
  username      String         @unique
  createdAt     DateTime       @default(now()) @db.Timestamptz(3)
  conversations Conversation[]
  documents     Document[]
}

model Conversation {
  id        String    @id @default(cuid())
  title     String?
  userId    String
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime  @default(now()) @db.Timestamptz(3)
  updatedAt DateTime  @updatedAt @db.Timestamptz(3)
  messages  Message[]
  documents Document[]

  @@index([userId])
}

model Message {
  id             String       @id @default(cuid())
  content        String
  role           Role // "user" or "assistant"
  createdAt      DateTime     @default(now()) @db.Timestamptz(3)
  conversation   Conversation @relation(fields: [conversationId], references: [id])
  conversationId String
}

model Document {
  id             String                      @id @default(cuid())
  title          String?
  userId         String
  user           User                        @relation(fields: [userId], references: [id], onDelete: Cascade)
  conversationId String?
  conversation   Conversation?               @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  contentHash    String?
  embedding      Unsupported("vector(1536)")
  metadata       Json?
  createdAt      DateTime                    @default(now()) @db.Timestamptz(3)
  chunks         DocumentChunk[]

  @@unique([userId, contentHash])
  @@index([userId, createdAt])
  @@index([conversationId])
}

model DocumentChunk {
  id         String                      @id @default(cuid())
  documentId String
  document   Document                    @relation(fields: [documentId], references: [id])
  content    String
  embedding  Unsupported("vector(1536)")
  section    String?
  page       Int?
  orderInDoc Int?
  metadata   Json?
  // Generated column: to_tsvector('english', content). Populated by the DB, not by
  // ingestion. See migration 20260701_add_fts_generated_column_and_gin_indexes.
  fts        Unsupported("tsvector")?
  createdAt  DateTime                    @default(now()) @db.Timestamptz(3)
  @@index([documentId])
  @@index([section])
  @@index([page])
  @@index([orderInDoc])
  // GIN indexes are declared here so Prisma tracks them (no drift), but they're created
  // in raw migration SQL alongside the fts generated column. The HNSW index on
  // `embedding` and the fts GENERATED expression stay raw — neither is expressible in
  // the Prisma schema.
  @@index([metadata], type: Gin)
  @@index([fts], type: Gin)
}

enum Role {
  user
  assistant
}
